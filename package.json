{
  "name": "openai-audience-refresh",
  "version": "1.0.0",
  "description": "Monthly refresh of the Buddy Punch customer exclusion audience in OpenAI Ads Manager (no audiences API — drives the UI headlessly with a stored session).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "playwright install chromium --with-deps || playwright install chromium"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@slack/web-api": "^7.8.0",
    "@supabase/supabase-js": "^2.45.0",
    "express": "^4.21.2",
    "node-cron": "^3.0.3",
    "playwright": "1.61.1"
  }
}
